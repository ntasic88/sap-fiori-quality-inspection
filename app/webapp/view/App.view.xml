<mvc:View
    controllerName="qi.app.controller.App"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    displayBlock="true">
    <App id="app" busy="{appView>/busy}" busyIndicatorDelay="{appView>/delay}">
        <pages>
        </pages>
    </App>
</mvc:View>
